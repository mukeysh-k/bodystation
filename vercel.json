{
  "_comment": "No cron jobs active right now — notifications are in-app only (see components/NotificationBell.tsx). When you're ready to add WhatsApp later, add this back: { \"crons\": [{ \"path\": \"/api/cron/reminders\", \"schedule\": \"0 9 * * *\" }] }"
}
